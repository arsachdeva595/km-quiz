{
  "name": "km-quiz",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "python3 pipeline/tag_ideas.py",
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 8000 --directory docs"
  }
}
